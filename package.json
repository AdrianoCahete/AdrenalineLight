{
  "name": "adrenaline-light-theme",
  "displayName": "Adrenaline Light Theme",
  "version": "1.1.0",
  "description": "Essa extensão foi criada para usarmos o Tema Light, enquanto o mesmo está desabilitado no forum da Adrenaline.",
  "homepage": "https://www.profolio.com.br/?l=adrenalinelight",
  "author": "Adriano Cahete",
  "settings": {
    "themeColor": "3c3c3c"
  },
  "engines": {
    "node": "~8.0.0"
  },
  "scripts": {
    "start": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "./node_modules/.bin/del-cli bin/*",
    "generate_artifacts": "node ./utils/zip.js",
    "// update_infos": "node ./utils/replace.js",
    "build": "npm run clean && npm run generate_artifacts"
  },
  "devDependencies": {
    "bestzip": "^2.1.4",
    "copyfiles": "^2.1.0",
    "del-cli": "^2.0.0",
    "replace": "^1.1.0"
  }
}
