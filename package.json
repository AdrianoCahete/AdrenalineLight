{
  "name": "better-adrena",
  "displayName": "BetterAdrena",
  "version": "1.2.0",
  "description": "BetterAdrena é uma extensão para o Chrome e Firefox que habilita uma versão alternativa do tema Light para o forum Adrenaline, junto com outras melhorias.",
  "homepage": "https://www.egaming.com.br/projetos/betteradrena",
  "author": "Adriano Cahete",
  "settings": {
    "themeColor": "3c3c3c"
  },
  "engines": {
    "node": "~8.0.0"
  },
  "scripts": {
    "start": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "./node_modules/.bin/del-cli bin/*",
    "generate_artifacts": "node ./utils/zip.js",
    "// update_infos": "node ./utils/replace.js",
    "build": "npm run clean && npm run generate_artifacts"
  },
  "devDependencies": {
    "bestzip": "^2.1.4",
    "copyfiles": "^2.2.0",
    "del-cli": "^2.0.0",
    "replace": "^1.1.0"
  }
}
